# copets

A collection of my Codex pets.

## Adding a pet

Create a pet directory with `pet.json` and `spritesheet.webp`, then regenerate the site data:

```sh
npm run generate:pets
```

The generated `pets.json` file is loaded by `index.html`.
